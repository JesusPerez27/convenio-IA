/** Mínimo de caracteres en la descripción inicial para clasificar. */
export const MIN_DESCRIPCION_CLASIFICAR = 10

/** Clave en `aportesPorAlerta` para un único texto unificado (revisión humana). */
export const APORTE_CONSOLIDADO_KEY = '__consolidado__'
